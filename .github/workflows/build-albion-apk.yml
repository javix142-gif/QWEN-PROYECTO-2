name: Compilar Albion Marketrelli APK

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Workflow temporalmente pausado durante preparación de corrección de arranque"
